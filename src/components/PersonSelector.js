

import React from "react";

export const PersonSelector = (props)=> (

<div className="person-selector-dropdown-container" style={{left:  `${props.position[0] - 20 }px`, top: `${props.position[1] - 20 }px`}}>
<div className="target-box"></div>

    </div>)

